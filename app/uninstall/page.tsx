// TODO: create table with columns: "Why are you uninstalling?", "Reason", "Date"
// TODO: Add a page that asks the user why they are uninstalling the extension
// Reference: https://simplify.jobs/uninstall

export default function UninstallPage() {
    return (
        <div></div>
    );
}